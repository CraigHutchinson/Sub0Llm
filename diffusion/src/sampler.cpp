// sampler.cpp — make_canvas / refine_canvas are now header templates in sampler.hpp (Ch32 P2 2e:
// templated on the model type so the SAME sampler drives both the flat Denoiser and the coarse-to-fine
// HierDenoiser). This translation unit is intentionally empty; kept for the build's source listing.

#include "sub0diff/nn/sampler.hpp"
