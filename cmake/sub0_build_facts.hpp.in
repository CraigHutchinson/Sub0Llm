#pragma once
// AUTO-GENERATED by CMake (configure_file @ONLY) -- do not edit.
// The build-known facts baked INTO sub0llm-configure so the CMake command stops mirroring them and the
// tool is self-sufficient when a user runs it directly: the detected device caps (CMake's CUDA existence
// check) and the generated-output paths (the build layout). See docs/WORKFLOW_ARCHITECTURE.md.
// These are the *defaults*; every one is still overridable by the matching CLI flag.

namespace sub0::build_facts {

inline constexpr int  HAS_CUDA      = @SUB0_BUILD_CUDA_INT@;  // 1 = the CUDA device backend was built
inline constexpr int  CUDA_ARCH     = @SUB0_CUDA_ARCH@;       // detected compute capability (e.g. 120 = sm_120)
inline constexpr int  GPU_VRAM_MB   = @SUB0_GPU_VRAM_MB@;     // dedicated VRAM (fast budget)
inline constexpr int  GPU_SHARED_MB = @SUB0_GPU_SHARED_MB@;   // WDDM shared/overflow (soft ceiling)

inline constexpr char GEN_HEADER[]     = "@GEN_HEADER@";      // default -o : the umbrella header to emit
inline constexpr char GEN_TUNE_CACHE[] = "@GEN_TUNE_CACHE@";  // default --tune-cache to fold in

}  // namespace sub0::build_facts
