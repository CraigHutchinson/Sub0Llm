// AUTO-GENERATED by sub0llm-specialize — DO NOT EDIT.
// Source GGUF: Qwen3-0.6B-Q8_0.gguf
// Architecture: qwen3  (family: qwen)
//
// Every field is a compile-time constant: include this header in a
// dedicated binary to monomorphize the model to these exact shapes.
#pragma once

#include <cstddef>
#include <cstdint>

namespace sub0llm::spec {

struct Qwen3_0_6B_Q8_0 {
    static constexpr const char* arch        = "qwen3";
    static constexpr const char* family      = "qwen";

    // ── dimensions ──────────────────────────────────────────────
    static constexpr int64_t     vocab_size   = 151936;
    static constexpr int64_t     embed_dim    = 1024;
    static constexpr std::size_t n_heads      = 16;
    static constexpr std::size_t n_kv_heads   = 8;
    static constexpr std::size_t head_dim     = 128;
    static constexpr int64_t     n_layers     = 28;
    static constexpr int64_t     d_ff         = 3072;
    static constexpr int64_t     context_len  = 40960;
    static constexpr float       rope_base    = 1e+06f;
    static constexpr float       norm_eps     = 1e-06f;
    static constexpr float       embed_scale  = 1.0f;

    // ── feature flags ───────────────────────────────────────────
    static constexpr bool use_qk_norm      = true;
    static constexpr bool tied_embeddings  = true;
    static constexpr bool norm_plus_one    = false;
    static constexpr bool attn_qkv_bias    = false;
    // 0 = SwiGLU (SiLU gate), 1 = GeGLU (GELU gate)
    static constexpr int  activation       = 0;
    static constexpr int64_t sliding_window     = 0;
    // 0 = uniform attention; N = every Nth layer is global (else windowed)
    static constexpr int  local_global_stride  = 0;

    // is layer l a global (full-attention) layer?
    static constexpr bool is_global_layer(int64_t l) noexcept {
        return local_global_stride == 0 || sliding_window <= 0
            || ((l + 1) % local_global_stride == 0);
    }

    static_assert(embed_dim > 0 && n_heads > 0 && n_layers > 0,
                  "specialized spec must have positive core dimensions");
    static_assert(n_heads % n_kv_heads == 0,
                  "n_heads must be a multiple of n_kv_heads (GQA)");
};

} // namespace sub0llm::spec
